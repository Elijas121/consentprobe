{
  "name": "consentprobe",
  "version": "0.1.0",
  "description": "Measure what a website really does before, and after, a visitor answers the cookie banner. Technical findings for GDPR/TDDDG work, not legal advice.",
  "author": "Elias Delil",
  "license": "MIT",
  "type": "module",
  "bin": {
    "consentprobe": "dist/cli.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!dist/demo.*",
    "!dist/fixtures.*",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prebuild": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "demo": "pnpm build && node dist/demo.js",
    "prepublishOnly": "pnpm typecheck && pnpm test && pnpm build"
  },
  "dependencies": {
    "playwright": "^1.63.0",
    "tldts": "^7.4.14"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  },
  "packageManager": "pnpm@11.5.1",
  "keywords": [
    "gdpr",
    "cookie-consent",
    "privacy",
    "playwright",
    "tdddg",
    "compliance",
    "cli"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Elijas121/consentprobe.git"
  },
  "homepage": "https://github.com/Elijas121/consentprobe#readme",
  "bugs": {
    "url": "https://github.com/Elijas121/consentprobe/issues"
  }
}
